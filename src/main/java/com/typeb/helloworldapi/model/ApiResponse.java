package com.typeb.helloworldapi.model;

/**
 * Keeping the class empty because the API is returning a simple JSON map at the moment
 * Implement this if a custom response format is required
 */
public class ApiResponse {
}
